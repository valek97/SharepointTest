'use strict';
$(document).ready(function () {
    // Добавление элемента ко всем параграфам после загрузки документа
    var test = '';
    $("div.header").append("<li class='item'>Тест</li>");
    $("div[class='main bg-white']").appendTo("");
});